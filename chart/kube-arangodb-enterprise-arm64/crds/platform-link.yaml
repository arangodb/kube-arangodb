apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: arangoplatformlinks.platform.arangodb.com
spec:
  group: platform.arangodb.com
  names:
    kind: ArangoPlatformLink
    listKind: ArangoPlatformLinkList
    plural: arangoplatformlinks
    singular: arangoplatformlink
  scope: Namespaced
  versions:
    - name: v1beta1
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
      served: true
      storage: true
      additionalPrinterColumns:
        - jsonPath: '.status.conditions[?(@.type=="Ready")].status'
          description: Defines if Connector is valid
          name: Ready
          type: string
      subresources:
        status: {}
