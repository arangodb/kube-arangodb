apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: arangopermissionpolicyrolebindings.permission.arangodb.com
spec:
  group: permission.arangodb.com
  names:
    kind: ArangoPermissionPolicyRoleBinding
    listKind: ArangoPermissionPolicyRoleBindingList
    plural: arangopermissionpolicyrolebindings
    singular: arangopermissionpolicyrolebinding
    shortNames:
      - arangopolicyrolebinding
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
      served: true
      storage: true
      additionalPrinterColumns:
        - jsonPath: '.status.conditions[?(@.type=="Ready")].status'
          description: Defines if PolicyRoleBinding is valid
          name: Ready
          type: string
      subresources:
        status: {}
